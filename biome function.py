def goback(x):
    t.penup()
    t.backward(x)
    t.pendown()
    
def biome():
    for i in range ():
        t.fillcolor("orange")
        t.begin_fill()
        for i in range(2):
            t.forward(60)
            t.left(90)
            t.forward (90)
            t.left (90)
            
        t.forward(70)
        t.end_fill()
#TOP LEVEL !!!!!!!        
import turtle as t
t.speed(0)
goback(300)
biome()
